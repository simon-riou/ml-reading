areas: [SOTA, training, generalization, robustness, computer-vision]
methods: [Residual-connections, ViT, CNN, LabelSmoothing, data-augmentation, LR-optimizations, TrivialAugment, RandomErasing, MixUp, CutMix, WeightDecay, FixRes-Mitigations, EMA, InferenceResize]
tasks: [convergence, regularization, classification]
status: [to-read, skimmed, deep-read, replicated]